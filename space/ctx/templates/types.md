## Vector

Fix type errors and improve type safety.

## Goals

- Eliminate all type checker errors
- Add type annotations to uncovered code
- Enforce strict type checking in CI
- Remove any/unknown type escape hatches

## Constraints

- Preserve existing type checker configuration
- Maintain backward compatibility
- Fix errors, don't suppress warnings
